# Design rules, and the fabrication class behind them

Generated by `rules.py`. Every number is read from JLCPCB's published capabilities page or derived from the two that are fitted; the quotations and the date read are in that file's docstring.

```
rules: 0.25 mm track, 0.2 mm clearance, 0.6/0.3 mm via, 2 oz outer
  routing pitch 0.5 mm = track + clearance + 0.05 mm margin; adjacent tracks 0.25 mm apart against 0.2 mm
  a via clears a track by 0.075 mm orthogonally (blocks) and 0.282 mm diagonally (clears)
  a TSSOP leaves 0.25 mm and its window is -0.40 mm -- -0.02 at the finest class -- so no track centre exists between two of its pins and every pin escapes outward
  and no cell inside one either: a pad holds a cell at every phase only above 0.70 mm of pitch, and a TSSOP is 50 um under it -- see rules.pad_reach() and design.ENV_ADC_CHANNEL
  a SOIC leaves 0.67 mm between pads:
      fitted        0.25/0.20 mm, either weight    window +0.020 mm against a 0.45 mm floor -- no cell fits, at any pitch
      2 oz minimum  0.15/0.15 mm, 2 oz outer       window +0.220 mm against a 0.30 mm floor -- no cell fits, at any pitch
      1 oz minimum  0.09/0.09 mm, 1 oz outer only  window +0.400 mm against a 0.18 mm floor -- a cell fits
```

## Does a routing cell fit between two SOIC pins?

The question the routing pass was left with, and the reason the answer turned out not to be a finer grid. A SOIC pad is 0.6 mm across on a 1.27 mm pitch, so there is 0.67 mm of copper-free space between two pins. A cell reliably falls in it only when the legal window is at least one grid pitch wide, and the finest legal pitch is `track + clearance` — so the test reduces to `gap >= 3 x clearance + 2 x track`, which does not mention the pitch at all.

| class | track / clearance | copper | window | pitch floor | a cell fits |
|---|---|---|---|---|---|
| fitted | 0.25 / 0.20 | either weight | +0.020 mm | 0.45 mm | **no**, at any pitch |
| 2 oz minimum | 0.15 / 0.15 | 2 oz outer | +0.220 mm | 0.30 mm | **no**, at any pitch |
| 1 oz minimum | 0.09 / 0.09 | 1 oz outer only | +0.400 mm | 0.18 mm | yes |

**So the corridor opens only at JLCPCB's finest multilayer class, which is available at 1 oz outer copper and not at 2 oz.** That is not a routing decision, it is a decision to give up 2 oz as an option. `route.route_all()` finishes the board without it, by ripping up and re-routing the nets that are in the way, so this board is ordered at the fitted class.
